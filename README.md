# Streamline Business Centre

Next.js landing page based on the provided design.

## Getting started

```bash
npm install
npm run dev
```

The main landing page is at `/` and each workspace/service slug has its own page
under `/[slug]` for the 21 services you listed.
